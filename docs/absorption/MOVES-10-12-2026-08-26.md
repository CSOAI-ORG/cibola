# MOVES 10–12 — live-cadence, C2PA/OTel binding, competition entries · 2026-08-26

## Move 11 — LiveBench live-cadence adoption + anti-gaming messaging (ADOPTED)
**The pattern:** contamination-resistant "live" benchmarks (LiveBench monthly, LiveCodeBench dated)
refresh items so models can't memorize. **Our estate already has the stronger property** — the
event core (event → recompute affected axes → sign → stream) mints **zero new verdicts on
identical evidence** (re-publishing identical evidence = no new card, verified anti-gaming).
**Adopt:**
1. Telemetry cadence: per-cycle measure stamps (already in `telemetry.jsonl` — add `cadence: live`)
   and date-versioned runs (LiveCodeBench-style `-YYYYMMDD` suffixes).
2. Anti-gaming messaging (public copy, grammar-locked):
   "Our cards are live: re-publishing identical evidence mints zero new verdicts. Contamination
   resistance is a property of our event core, not a promise about a frozen set."
3. Wire into the LIVE-vs-PLANNED labeling discipline (never call a live-run artifact static).

## Move 12 — C2PA + OpenTelemetry binding note (C2PA Contributor Member — the seam)
- **C2PA**: we hold Contributor membership (docusign 7C9592DB). The seam: a measurement card's
  payload digest becomes a C2PA claim in an asset's Content Credentials manifest — so provenance
  and *evaluation* travel together (the Art 50(2) machine-readable watermarking deadline 2 Dec 2026
  is the demand trigger). Implementation: `c2pa-rs` (verify license before use — flagged in the
  absorption manifest) binding the card digest into an `assertion`; signer = our did:web.
- **OpenTelemetry GenAI semantic conventions (Apache-2.0)**: map `gen_ai.*` spans to card fields
  (model, provider, prompt digest, completion digest, latency, cost) so a card is *telemetry-bound*,
  not just score-bound. Adopt OTel attribute names in `telemetry.jsonl` for drop-in interop.
- Deliverable: `docs/absorption/C2PA-OTEL-BINDING.md` spec → build when the sweep deploy window opens.

## Move 10 — Competition entries (staged, doctrine-compatible, cash prizes)
| Competition | Deadline | Status | Asset |
|---|---|---|---|
| **ARC Prize 2026 (ARC-AGI-3 + ARC-AGI-2)** | Submissions close **Nov 2 2026**; results Dec 4 | ✅ draft in docs/fire/FIRES-BUNDLE (paper) + entry plan below | DeepSeek guardrail harness + agent; offline (no external API); open-source-to-win (MIT/CC0) — doctrine-clean |
| **HackAPrompt 2.0 (× OpenAI)** | Rolling through 2026 | 🟡 entry notes staged | red-team = jail axis bank (8 families) — findings feed signed jail cards |
| AIMO3 | ended Apr 15 2026 | ✅ n/a (past; lesson: enter early next cycle) | — |
| Gray Swan Safeguards | Feb–May 2026 (past) | ✅ n/a; next waves recurring | harness red+blue |

**ARC entry plan (one paragraph):** submit the guardrail harness as an offline agent (no external
API) on ARC-AGI-2/3, target the Jun 30/Sep 30 milestones ($25K/$10K/$2.5K); open-source the solution
per rules (MIT/CC0 — consistent with "we measure, we sign"); every milestone result lands as a
signed card on the board (measure-and-compete, never certify). Milestone entry scoring in the bottom
quartile → pivot to "measure-and-publish" framing (still doctrine-valid).

## Move 8 remainder — Sigstore/Rekor (staged, honest)
- OTS shipped LIVE this session. Rekor keyless needs OIDC/identity flow — staged for the pod lane;
  in-toto/SLSA attestation formats mapped (absorption manifest row). Do NOT claim Rekor live until
  the first proof commits.

*Everything above is committed evidence; nothing sent externally. Mints/deploys = pod window +
owner window.*
