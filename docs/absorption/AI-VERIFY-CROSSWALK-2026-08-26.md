# AI Verify + Project Moonshot — crosswalk note (move 7 part 4) · 2026-08-26

**Peer:** Singapore IMDA AI Verify + Project Moonshot — the national-government open-source
toolkit (process + LLM red-team/benchmark). Doctrine-clean to absorb + crosswalk (Apache-leaning).

**Crosswalk rows (measurement, never certification):**
- AI Verify process checks ↔ our governance/care axes (org-process vs model-measurement = complement)
- Moonshot red-team benchmark ↔ our jail axis (8-family bank) — mutual test sets, signed results
- Both publish honest artifacts; our signed-card layer is the verifiable complement neither ships

**Action:** absorb toolkit (license verify first), publish a signed AI-Verify crosswalk card at the
deploy window; interoperates with the direct national-government peer — international credibility.
