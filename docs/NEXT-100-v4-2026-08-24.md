# NEXT 100 — v4 (2026-08-24) · WEEK-1 EXECUTION PLAN (Aug 24–30)

*Operationalises NEXT-100-v3 (2026-08-23). State below probed live. Batch-run list at bottom = executed this session.*

## STATE (probed 24 Aug)
- Pods: 3090 `sov-repull` RUNNING (signing/measure) · **A100 `sovos-light-master-mine-20260816` RUNNING** (fleet flag RESOLVED — 1 A100 alive; 10 exited incl. all takeover/council-ring/overnight-bench) · CONNECT-SPRAY §D1 stale now closed
- EAT overnight-300: cycle 4 done (ok 25/failed 3 in-cycle; 116 steps / 104 ok overall), chain 3,702 · 0 breaks
- GitHub org verified live: `CSOAI-ORG/cibola` (public, CIBOLA=protocol name — **decision log 001 landed**; DORADO = internal codename) · `councilof-ai` (flagship public) · `councilof-ai-monorepo` (55/55 packages) · `claimguard` · `corpus-watch` (regulatory drift watcher) · `sim-world-estate` (private) · `sovos-harness` (private)
- gh CLI authed as CSOAI-ORG ✓ · runpodctl ✓
- Canon targets: `/council` → councilof.ai/council/ 200 ✓ · `/gspc-verify` → 200 ✓

## WEEK THEME: **first mover in the verification gap — three deadlines, one proof**
(1) DRCF Phase 2 ⏰ Sep 2 · (2) AI Office transparency consultation + Art 50(7) process live · (3) IANA expert clock (~2 wks). All three converge on: *signed, stranger-verifiable artifacts with an empty competitive field.*

---
## A — AVENUES: complete the live flag (1–18)
1. ⏳ Genesis card POD-signed on the pod (kid=real pod key, not test-identity) — verify on clean machine
2. ⏳ I-D -00 datatracker: confirm submission (draft exists @docs/draft/draft-csoai-scitt-measurement-card-00.txt); if absent → SUBMIT (free)
3. ⏳ IANA media-type form: `application/vnd.cibola.measurement-card+json` — submit + media-types@ post same day
4. ⏳ Dual-TS: register genesis card in 2nd independent transparency service + cross-verify matrix in CI
5. ⏳ Verify page: paste-card → sig → receipt → Merkle path (stranger walkthrough, 60s timer)
6. ✅ Frozen vectors v1 (valid/bad-sig/bad-receipt) hash-pinned + CI — `scripts/gen-frozen-vectors.py` + `test/frozen-vectors.py` + CI drift-check; deterministic across runs
7. ✅ REGISTRY.md: registry of record rows + did:web-signed update flow (in-repo doc — board = registry of record; did.json key list + additive rotation; see `REGISTRY.md`)
8. ✅ Purge pass 2: banned strings on cibola/councilof-ai public repos (SOVOS · SOV OS · SOV4 · SOV33 · SOVEREIGN) via gh code search — CI lint red-on-hit — `test/banned-strings.py` now covers README + staged week-1 external text
9. ⏳ `corpus-watch` (EU/UK statutory drift) → wire its daily hash output into the compliance calendar (Art 72/73 guidance changes auto-fire)
10. ⏳ Art 73 signed incident-feed prototype: emitter CLI e2e + pre-commitment terms (ASRS structure)
11. ⏳ Art 72 PMM daily signed stream (3-day sim) + threshold auto-notify demo
12. ✅ SB 315 machine-readable summary emitter + auditor-card template (Jan 2028 market credibility NOW)
13. ⏳ East-west bridge card demo: EU / US-IL / CN readings side-by-side on verify page
14. ⏳ Watermark harness dry-run on synthetic fixture + detector-gap watch page (Anthropic detector announced, unreleased)
15. ⏳ Signed-recipe gate MVP: Claude Code PreToolUse hook + 4/4 bypass drill (unsigned/mis-signed/expired/replayed)
16. ⏳ E2E grand walkthrough recorded: measure → card → POD sign → dual-TS → verify page → bridge reading
17. ✅ Stranger-verification counter + verify-kit bundle (card+payload+receipts+keys,
    offline) — `harness/verify_kit.py` + `test/verify-kit.py` + `cli/dorado.py verify-kit`;
    append-only verification ledger + caller-trusted vs kit-bundled identity pin
18. ⏳ 72h-done report + POD-signed artifact manifest (the proof inventory)

## M — MINE the moat (19–30)
19. 🔄 EAT continue → next 100 cycles (verify-all 11/11 each; no duplicate daemons — one cadence owner)
20. ⏳ Pod-native EAT batch on `sovos-light-master-mine` (A100 now alive — windowed, GPU-flush preflight)
21. ⏳ Telemetry.jsonl per measure: model/provider/cost/latency/tok/s (OpenRouter differentiator, DORADO Phase 0)
22. 🟡 OpenSpiel envs +2 — ✅ LANDED (nim, wythoff, determinism-gated); per-axis
     stratification (16 axes balanced) — ⏳ STILL PENDING (needs a balanced probe pool,
     tied to the move-23/24 item-bank growth; not agent-doable until the pool exists)
23. ⏳ GovBench item bank growth (held-out = trade secret, never published)
24. ⏳ Register→card seed: regulator-deadline + platform unknowns → h3k cards
25. ⏳ Player-voice mining, no-LLM-judge probes (invariant grep)
26. ⏳ DSA per-platform artifact inspection → fill 23 `unknown` booleans (fan-out research, subagents)
27. ⏳ Embodied-transparency records + OSA register stub (Ofcom categorisation register watch)
28. ⏳ Corpus-watch -> honey fuel (statutor drift records → gspc drift axis)
29. ⏳ Kaggle/OpenML/HF pushes per cycle (private unless deliberate)
30. ⏳ 3090 contention rule codified: measurement windows need GPU-flush preflight; RealPDE training niced below measure_chain

## A2 — AUDIT everything (31–44)
31. ✅ SCITT COSE_Sign1 verify (not just count) — RFC 9052 CBOR verifier, stranger-only
    (`harness/scitt_verify.py` + `test/scitt-verify.py` + `cli/dorado.py scitt`); honest
    pinned/self-consistent hierarchy; landed with move 17
32. ⏳ Register-per-record sig verify (68 records / 7 registers) + determinism re-run diff vs published
33. ⏳ Corrections C-23+ review (append-only asserted)
34. ⏳ Licence landmine sweep on all new deps (3 hard-excluded families)
35. ⏳ Disk/reboot re-verify (Mac floor; pod snapshots; Oracle target)
36. ⏳ Game determinism: double-run → identical receipt hash (unsealed-never-signed honored)
37. ⏳ AIP-01 patch staged (corrections doc) — raise after owner nod
38. ✅ Sprint-300 reconcile: which of critical-10 landed (honest table — `docs/sprint-300-critical-10-reconcile-2026-08-24.md`; 2 landed, 1 partial, 1 staged, 6 external/owner-gated/sibling)
39. ✅ Banned-string + grammar lint on all week-1 external text pre-send — `test/grammar-lint.py` sentence-aware; public + staged text honoured
40. ✅ Production 308 canonical all-target map (recorded in repo — `docs/production-308-canonical-map-2026-08-24.md`; csoai.org→councilof.ai 308 method-preserving; www.councilof.ai subpath gap flagged)
41. 🔄 Chain-break + feed-stomp watch (self-heal confirmed ~6min; don't merge onto sibling branch)
42. ⏳ Register index vs files; HF-pushed vs local match
43. ⏳ Scenario receipt / JCS payload-binding stranger-verify
44. ⏳ Determinism gate on new OpenSpiel envs (CI)

## T — TEST as end user (45–56)
45. ⏳ All games (7): seed→replay→receipt→stranger-verify (5 original + nim/wythoff)
46. ⏳ Jail bank whole-run: refusal/over-refusal signed scenario receipt (8/8 families)
47. ⏳ Cold-start e2e fresh VM (zero our-infra assumptions)
48. ⏳ GUI click-through 16 tabs (Council OS) incl. register viewer
49. ⏳ /lookup/<hash> browser-only verify
50. ⏳ Reboot-survival drill (bootout/bootstrap, KeepAlive)
51. ✅ Rate-cap/velocity attestation self-test — `harness/rate_cap.py` +
     `test/rate-cap.py` + `cli/dorado.py velocity`; honest over-cap reporting (never
     clipped), deterministic window partition, signed + stranger-verified, register rides
     the card (measurement, never certification)
52. ✅ GB/T 45654 gates encode self-check (≥90% / ≤5% / ≥2k / ≥10k)
53. ⏳ Verify page 100rps load + cache headers
54. ⏳ e2e single CLI (json, fail-fast, per-section ids, time budgets)
55. ⏳ Sim tools (sim_scene/sim_benchmark) smoke post-restart
56. ⏳ C-16 tick cumulative + world display live

## I — IMPROVE (57–70)
57. ⏳ MCP #426 conformance fix: re-anchor PR with 2026-07-28 adoption links (Claude + AgentCore Gateway) + before/after table
58. ⏳ AG-UI audio proposal: issue + PR + dojo E2E tests; 14-day fallback trigger armed
59. ⏳ Inspect (MIT) signed-receipt scorer hook
60. ⏳ DOI-cited Zenodo dataset (release-to-DOI rail proven — run for next carder cycle)
61. ⏳ Registry page + standards-engagement log footer-linked (csoai.org)
62. ⏳ A2A/MCP `.well-known` discovery live on production surface
63. ⏳ HF gspc-boards cards-index + board-live schema
64. ⏳ Council OS browser-bridge tab for signed feeds (7 registers)
65. ✅ "13 measured of 14" grammar everywhere public (verified-credential language, zero certification) — `test/grammar-lint.py` now guards the completeness over-claim ("14 of 14" / "all 14") over PUBLIC + staged text + `--selfcheck` fixtures
66. ⏳ Human-bridge league design doc v0.1 + futures-sim credibility plan (prereg stubs)
67. ⏳ Blind/sequestered run design: hidden-set hash commitments before runs (self-applied AITE pattern)
68. ⏳ Watermark harness baseline row: reproduce Anthropic disclosed limits for comparability
69. ✅ Driver: one-instrument doc (single canonical description of the instrument)
70. ⏳ Nomination: board/committee involvement audit (C2PA done; OpenSSF/OWASP/AI Verify state check)

## F — SHIP this week (71–78)
71. ⏳ Verify page + genesis card public (stranger-checkable from any device)
72. ⏳ Standards-engagement log live (I-D · IANA · list posts · PRs)
73. ⏳ AIP-01 corrections raised (after nod) — credibility deposit
74. ⏳ Federated feed endpoints public: /feeds/incidents /feeds/pmm (push stub + pull endpoint)
75. ⏳ Registry page live on councilof.ai
76. ⏳ 72h-done report + manifest published internally (webpage for CRM)
77. ✅ README rename-split notification: CIBOLA public / DORADO internal (one paragraph, cibola README)
78. ⏳ Week-1 metrics row on dashboard (cards, verifications, PRs, submissions, watches)

## W — WATCH + windows (79–88)
79. 🔄 AI Office transparency guidelines consultation → response drafted (ART50 doc, staged; send inside window)
80. 🔄 Digital Omnibus Reg 2026/174 implementing acts (Art 72 PMM + Art 73) — guidance watch armed
81. 🔄 Anthropic detector release → fires watermark harness
82. 🔄 MCP #426 thread + spec releases (reply <24h)
83. 🔄 IETF 126→127 two-TS interop demo plan + hackathon recruitment
84. 🔄 IANA expert review replies (<24h SLA)
85. 🔄 **DRCF Phase 2 ⏰ 2 SEP** — review packet → NICK send
86. 🔄 BSI ART/1 seat — NICK as named expert
87. 🔄 Compliance calendar rows re-verified (ECCTA Nov 18 · Art 50(2) Dec 2 · VN Mar/Sep 2027 · IL Jan 2028)
88. 🔄 Benchmark-exposé watch (any new cheater story → refresh packs in 24h)

## C — CONNECT (89–96)
89. ⏳ METR pack v2 (DRAFT READY — GPT-5.6 Sol hook) → owner send
90. ⏳ TB/Harbor pack v2 (DRAFT READY — provenance spine) → owner send
91. ⏳ NIST AITE co-sign pack (complementary: sequestration + wrapper; 3–6mo expectation)
92. ⏳ C2PA co-member warm-intro offer (content-provenance × eval-provenance crosswalk)
93. ⏳ IJCAI/CoG + AI Arena/SC2AI signed-replay pitch + AIP corrections thread (agent-protocol credibility)
94. ⏳ Tracxn profile claim/enrich + Crunchbase/PitchBook/Dealroom (ranked 104/1,061 — capitalise)
95. ⏳ Inngot/Goldseam follow-through: confirm programme status; use IP profile in insurer fund pack (⏰ Sep 30)
96. ⏳ Insurer meeting pack staged (deck + ledger + bands $15–50k/$25–300k/$75–400k + sample card)

## B + IP (97–100)
97. ⏳ Art 50 readiness product + rate card (€8–80k) — revenue track first
98. ⏳ UKIPO: COUNCIL OF AI 4 classes + MEOK 2 classes submissions (staged → 🔒 NICK £)
99. ✅ OIN scope check signed off (card format = non-Linux-kernel-adjacent, clean) + LOT Network membership status confirm
100. 🔴 IP notices commit + footer-wire (DB right over honey KB + benchmark-results; split-salt public notes)

---
## BATCH RUN — executed 2026-08-24 (initial session)
- ✅ Pod fleet verified (3090 + 1 A100 RUNNING; 10 EXITED) — closes CONNECT-SPRAY §D1
- ✅ Naming decision reconciled against live org (CIBOLA public / DORADO internal / COUNCIL OF AI brand) — v3 move 97 corrected; repo-rename todo DELETED
- ✅ Drafted Art 50 transparency consultation response (docs/outreach/)
- ✅ Drafted METR pack v2 + TB/Harbor pack v2 (owner-send ready)
- ✅ Staged AIP-01 corrections (raise after nod)
- ✅ v4 written; committed+ pushed to CSOAI-ORG/cibola (docs/)
- ✅ Health sweep: councilof.ai/council/ + /gspc-verify/ 200

## BATCH RUN — ralph round 1 (this round, 2026-08-24)
- ✅ Move 6 — frozen vectors v1 (valid/bad-sig/bad-receipt) hash-pinned + CI drift-check.
  `scripts/gen-frozen-vectors.py` + `test/frozen-vectors.py`; deterministic across runs
  (fixed test key, frozen timestamp); 8/8 checks green. Wired into `.github/workflows/ci.yml`.
- ✅ Move 8 — purge pass 2: `test/banned-strings.py` extended to README.md + the three
  staged week-1 external texts (METR/TB-Harbor packs + ART50 response).
- ✅ Move 39 — `test/grammar-lint.py`: sentence-aware binding-canongrammar guard over
  public surfaces + staged week-1 text (never a positive certification/accreditation claim;
  register negations + the legitimate "regulators and accredited bodies" reference allowed).
  Wired into CI.
- ✅ Move 77 — README.md naming split paragraph (CIBOLA public / DORADO internal /
  Council of AI body brand; repo intentionally not renamed).

## BATCH RUN — ralph round 2 (this round, 2026-08-24)
- ✅ Move 7 — `REGISTRY.md` (repo root): registry-of-record rows (board = append-only
  content-addressed hash-chained measurement register) + the did:web-signed update flow
  (active `kid` list, additive rotation / supersede, key-continuity rule, stranger
  verification path). Grounded in the live `councilof.ai/.well-known/did.json`.
- ✅ Move 40 — `docs/production-308-canonical-map-2026-08-24.md`: probed (read-only curl)
  canonical all-target map across councilof.ai / csoai.org / www variants. Confirmed
  csoai.org→councilof.ai 308 is method-preserving; flagged the `www.councilof.ai` subpath
  canonicalization gap + `csoai.org/api/methodology` 404 flapping (not fixed — deploy forbidden).
- ✅ Move 65 — `test/grammar-lint.py` extended: a "13 measured of 14" completeness over-claim
  guard (flags "14 of 14" / "all 14" as a positive claim; allows "13 of 14", a smaller
  domain registry's "N measured of M", and the normative "never '14 of 14'" register).
  `--selfcheck` fixture mode added. Wired into `.github/workflows/ci.yml` (lint + selfcheck).
- ✅ Move 38 — `docs/sprint-300-critical-10-reconcile-2026-08-24.md`: honest critical-10 table
  (2 landed, 1 partial, 1 staged, 6 external/owner-gated/sibling; each with repo/live evidence).
  Verify page confirmed live (`/verify` → 308 → `/gspc-verify/` → 200); genesis card (item 1)
  confirmed still pending (board rows here are `#test-identity`).

## BATCH RUN — ralph round 3 (this round, 2026-08-24)
- ✅ Move 52 — `harness/gbt_gates.py` + `test/gbt-gates.py` + `cli/dorado.py gates`: the
  GB/T 45654-style credibility gates (calibration ≥ 90% / over-refusal ≤ 5% / per-axis n ≥ 2k
  / total n ≥ 10k) encoded as a deterministic, hermetic self-check with a `--fixture` smoke.
  A below-floor value is reported honestly and the run is marked NOT QUOTABLE (a datum,
  never a finding); the measurement-credential register rides the result. Wired into CI.
- ✅ Move 12 — `harness/sb315.py` + `test/sb315.py` + `cli/dorado.py sb315`: a signed
  measurement card becomes a machine-readable transparency summary
  (`csoai.transparency-summary/0.1`) + a stranger/auditor walkthrough template
  (`csoai.auditor-card-template/0.1`), both bound to the card's canonical digest
  (content_sha256 + signed/kid). Honest "N measured of M" completeness grammar; excluded
  axes reported honestly; measurement-credential register carried. Wired into CI.
- ✅ Move 69 — `docs/one-instrument-2026-08-24.md`: the single canonical description of the
  DORADO measurement instrument (pipeline, axes, score layer vs RFC 9943 substrate,
  neutrality doctrine, credibility gates, completeness grammar, what it is NOT).
- ✅ Move 99 — `docs/oin-scope-check-2026-08-24.md`: the mandatory OIN Linux-System scope
  check against the measurement-card format + instrument IP. Result: NOT
  Linux-kernel-adjacent → clean to file under OIN 2.0 (Section 1.2 grant-back does NOT
  apply); crown-jewel invariants reaffirmed; LOT Network membership recorded honestly as
  submitted/pending (not claimed granted).
- New hermetic tests wired into `.github/workflows/ci.yml` (GB/T gates + SB 315); the
  `INTERNAL_MODULES` allowlist in `test/licence-sweep.py` now recognizes the two new
  harness modules as estate-internal. Full deterministic battery + all CI hermetic tests green.

## BATCH RUN — ralph round 4 (this round, 2026-08-24)
- ✅ Move 31 — `harness/scitt_verify.py` + `test/scitt-verify.py` + `cli/dorado.py scitt`:
  SCITT COSE_Sign1 CRYPTOGRAPHIC verify (not just count). Decodes the CBOR COSE_Sign1
  envelope (RFC 9052/RFC 9943, alg -19 Ed25519), reconstructs the Sig_structure, and checks
  the Ed25519 signature over it with `cryptography`. Stranger-only, no network, no pod.
  Tamper (payload/sig/protected header) and wrong caller-pinned key rejected; a non-published
  kid is reported 'self-consistent but NOT pinned' (never verified-authentic); permit_unpinned
  downgrades, never upgrades; batch counts verified/failed/unverifiable separately.
  Deterministic (seeded test key). Wired into CI + `cbor2` declared as a permissive dep.
- ✅ Move 17 — `harness/verify_kit.py` + `test/verify-kit.py` + `cli/dorado.py verify-kit`:
  offline stranger verify-kit bundle (card + payload + receipt + anchor + did:web keys +
  walkthrough in ONE deterministic artifact) + whole-kit offline verify (digest/card/receipt/
  anchor/identity-pin, per-part verdicts) + an append-only verification COUNTER
  (`data/verify-log.jsonl`, usage/provenance never validity). Identity pin labelled
  caller-trusted vs kit-bundled so self-bundled keys are never mistaken for an
  independently-fetched did:web authentication. Deterministic, no network. Wired into CI.

## BATCH RUN — ralph round 5 (this round, 2026-08-24)
- ✅ Move 22 (OpenSpiel envs +2, determinism-gated) — `engine/game_replay.py` + two new
  deterministic impartial combinatorial games: `nim` (3-heap take-away, normal play) and
  `wythoff` (two-pile take-away, normal play — the golden-ratio cold positions). Both are
  integer-only / JSON-serializable (JCS-bindable), seed-sensitive, and pass the FULL
  move-44 determinism gate in `test/game-replay.py` (seed->replay determinism, JCS payload
  binding, double-run identical receipt content_id/digest, a different-seed/altered transcript
  NOT bound, measurement-credential register carried). `list_games()` now returns 7; the test
  asserts the canonical 5 are present and EVERY env in the (extensible) set passes the gate.
  Per-axis stratification (the other half of move 22) is STILL PENDING — it needs a balanced
  probe pool that does not yet exist (tied to the move-23/24 item-bank growth), so it is not
  agent-doable until the pool exists.
- ✅ Move 51 — `harness/rate_cap.py` + `test/rate-cap.py` + `cli/dorado.py velocity`:
  issuance-velocity / rate-cap attestation self-test. The estate attests its OWN issuance
  velocity honestly: a per-subject OR global over-cap window is reported VERBATIM (list of
  {window, subject, count, cap} violations) — it is never silently clipped, re-rated, or
  hidden (anti-Goodhart / nobody-ranked-pays). The controller derives its window partition
  ONLY from event timestamps (no wall clock), so the same ledger always yields the same
  windows; `clipped: false` always. Emits a deterministic `csoai.velocity-attestation/0.1`
  card, signed (test identity in the hermetic tests) and STRANGER-verified via `cryptography`,
  carried by the measurement-credential register (a self-audit record, never a certification).
  No LLM-judge; hermetic + off-network. Wired into CI.
- Both wired into `.github/workflows/ci.yml`; `rate_cap` added to the `INTERNAL_MODULES`
  allowlist in `test/licence-sweep.py`. Full deterministic battery + all CI hermetic tests green.

## WATCHWORDS FOR THE WEEK (grammar, binding)
credential-never-certification · "13 measured of 14" · nobody-ranked-pays · no LLM-judge · signed-only ratings · honest unknown · never claim accreditation before granted · buyer-side money only.
