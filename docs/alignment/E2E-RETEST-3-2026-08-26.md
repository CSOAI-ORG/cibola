# E2E-RETEST-3 — 30/30 PASS against the 22-axis standard · 2026-08-26

**Scope:** END-USER-STANDARD acceptance re-run AFTER the 22-axis sweep landed.
Method: `curl -s -L -w %{http_code}` read-only; 30 checks.

## Results
- **22 axis pages (14 GSPC + 8 financial): 22/22 PASS** — all reachable at /gspc/<slug> (8 were 404 pre-sweep).
- **Persona/machine surfaces: 8/8 PASS** — /api/gspc · /gspc-verify · /api/mcp · /os · /evidence ·
  /crosswalks · .well-known/agent.json · .well-known/did.json.
- **SCORED: 30/30 PASS, 0 FAIL.**

## Zenodo DOI rail (owner-gate execution, 26 Aug)
- Deposit **22113338** created via API (ZENODO_TOKEN): "Rating the Raters 2026 — measurement of AI
  benchmark raters (signed card)", CC-BY-4.0, state unsubmitted, 0 files.
- Upload (415/500) + publish pending — retry or use the proven C3 rail (gh release → Zenodo
  auto-DOI). Do not claim the DOI until published.

## Pod production-signing (owner-gate execution attempt, 26 Aug)
- SSH to signing pod `sov-repull-20260808` CONNECTED (port 12473, runpodctl key).
- Board kid=card-attestation-1 confirmed pod-side (production signing exists).
- Signing key is keystone-held (estate vault, "keys never leave the pod") — NOT poked; the
  keystone path is the lane's window. The 4 locally-minted cards await production re-sign there.
